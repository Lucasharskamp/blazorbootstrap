<Alert @ref="warningAlert" Color="BsColor.Warning">
    <strong>Holy guacamole!</strong> You should check in on some of those fields below. <Button Color="BsColor.Primary" @onclick="(async () => await warningAlert.CloseAsync())">Close</Button>
</Alert>

@code {
    Alert warningAlert = default!;
}
